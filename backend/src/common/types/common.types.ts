export type EnvConfig = {
  DB_HOST: string;
  DB_PORT: string;
  POSTGRES_USER: string;
  POSTGRES_PASSWORD: string;
  POSTGRES_DB: string;

  REDIS_HOST: string;
  REDIS_PORT: string;
  REDIS_USER: string;
  REDIS_PASSWORD: string;
};
